import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ComponentScreen = () => {
    return (
        <Text>This is my component screen</Text>
    )
};

const style = StyleSheet.create({

});

export default ComponentScreen;